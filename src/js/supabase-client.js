// TODO Supabase: install @supabase/supabase-js and replace this file with a real client.
//
// Example wiring once credentials are available:
//
//   import { createClient } from '@supabase/supabase-js';
//
//   const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL;
//   const SUPABASE_ANON_KEY = import.meta.env.VITE_SUPABASE_ANON_KEY;
//
//   export const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY);
//
// Add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY to a .env file at project root
// (and add `.env` to .gitignore — it isn't there yet).

export const supabase = null;
